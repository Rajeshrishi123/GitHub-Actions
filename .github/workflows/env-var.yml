name: env-demo
on: workflow_dispatch
env:
 env_name: Dev
  

jobs:
 env-demo:
 env:
  env_name: Test
  runs-on: ubuntu-latest
  steps:
   - name: Env vars calling
     run: echo " ${{env.env_name}} env"

     
