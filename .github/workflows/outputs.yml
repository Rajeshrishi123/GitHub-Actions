name: outputs-demo
on: push

jobs:
 outputs:
  runs-on: ubuntu-latest
  steps:
   - name: first-step
     id: step1
     run: echo "Version is 0.0.1" >> $GITHUB_OUTPUT

   - name: second-step
     run: echo "application is deployed using version ${{steps.step1.outputs.version}}"
  
     
